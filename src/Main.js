import React from "react"

const Main = ({controleLogout}) => {
    return(
    <section className="main">
        <nav>
            <h2>APU</h2>
            <button>Entrar</button>
            
        </nav>
    </section>
    )
}

export default Main;